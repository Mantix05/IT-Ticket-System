import { Routes } from '@angular/router';
import { TicketDashboard } from './pages/ticket-dashboard/ticket-dashboard';

export const routes: Routes = [
    { path: '', component: TicketDashboard }
];
    